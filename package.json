{
  "name": "cds-debug",
  "displayName": "CDS Debug",
  "description": "Debug multiple SAP CAP services simultaneously via Cloud Foundry integration",
  "version": "0.3.62-pre.14",
  "icon": "resources/icon.png",
  "publisher": "dongtran",
  "repository": {
    "type": "git",
    "url": "https://github.com/dongitran/CDS-Debug.git"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.112.0",
    "node": ">=20"
  },
  "categories": [
    "Debuggers",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "cds-debug",
          "title": "CDS Debug",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "cds-debug": [
        {
          "type": "webview",
          "id": "cdsDebug.mainView",
          "name": "Debug Launcher"
        }
      ]
    },
    "configuration": {
      "title": "CDS Debug",
      "properties": {
        "cdsDebug.tunnelReadyTimeoutSeconds": {
          "type": "number",
          "default": 30,
          "minimum": 10,
          "maximum": 120,
          "markdownDescription": "How long (in seconds) to wait for the SSH debug tunnel to become ready before reporting a connection error. Increase this value for high-latency CF regions (e.g. `ap10`, `jp10`) or slow app start-ups. Default: `30`."
        },
        "cdsDebug.warnRemoteInspectorAfterStop": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Opt in to a reminder after stopping a debug session that the remote Node inspector can remain open inside the CF app until the app restarts. Disabled by default because restarting Cloud Foundry apps can be slow or disruptive."
        },
        "cdsDebug.autoRestartAppAfterStop": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Automatically run `cf restart <app>` after stopping a debug session to close the remote Node inspector completely. This can reset in-memory app state and take time, so it is opt-in."
        },
        "cdsDebug.warnDebuggerLiteralOnAttach": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Scan local source for `debugger;` statements after attach and warn if found. These statements pause the app whenever any inspector client is connected."
        },
        "cdsDebug.clearRemoteBreakpointsBeforeStop": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Send defensive `setBreakpoints([])` requests before stopping a CDS Debug-managed session, reducing reliance on adapter/socket cleanup alone."
        },
        "cdsDebug.tunnelKeepaliveIntervalSeconds": {
          "type": "number",
          "default": 10,
          "minimum": 0,
          "maximum": 60,
          "markdownDescription": "How often CDS Debug sends a lightweight `threads` request to detect a half-open inspector tunnel. Set to `0` to disable keepalive recovery."
        },
        "cdsDebug.signalAllNodeProcesses": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Use the legacy `kill -s USR1 $(pidof node)` behavior that signals every Node process in the CF container. By default, CDS Debug targets a likely main process and excludes common sidecars."
        },
        "cdsDebug.pauseOnBreakpoint": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "When `Breakpoint Snapshot Handling` is enabled in the CDS Debug Settings panel, turning this on keeps the debugger paused at breakpoints. When off, CDS Debug captures breakpoint context and auto-continues. Breakpoint snapshot handling itself is off by default."
        },
        "cdsDebug.breakpointSnapshotMaxEntries": {
          "type": "number",
          "default": 120,
          "minimum": 20,
          "maximum": 1000,
          "markdownDescription": "Maximum number of breakpoint context snapshots kept in memory for the webview list."
        },
        "cdsDebug.packageRegexFilter": {
          "type": "string",
          "default": "",
          "markdownDescription": "Default regex filter applied to package names in the Packages screen. This acts as an initial value and can be overridden per session via the extension UI. If modified in the UI, the extension will attempt to update the appropriate scope (Workspace or User) if it was previously set there."
        },
        "sapCap.currentScope": {
          "type": "object",
          "scope": "application",
          "markdownDescription": "Currently active Cloud Foundry scope managed by CDS Debug.",
          "properties": {
            "regionCode": { "type": "string" },
            "orgName": { "type": "string" },
            "spaceName": { "type": "string" }
          }
        },
        "cdsDebug.sharedCapDebugConfig": {
          "type": "object",
          "scope": "application",
          "default": {},
          "additionalProperties": false,
          "markdownDescription": "Shared fallback for `cap-debug-config.json`. When configured in user settings, CDS Debug uses this object before checking workspace `.vscode/cap-debug-config.json`. Per-service `cap-debug-config.json` files still take priority.",
          "properties": {
            "remoteRoot": {
              "type": "string",
              "markdownDescription": "Remote source root used for generated attach configurations. Use a fixed path such as `/home/vcap/app`, or an explicit regex with `regex:<pattern>` or `/pattern/flags`; regex values are resolved per CF app through `cf ssh` before debugging starts."
            },
            "branch": {
              "type": "string",
              "markdownDescription": "Default branch to use for branch preparation when no per-service `branch` is configured."
            },
            "orgBranchMap": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              },
              "markdownDescription": "Maps Cloud Foundry org names to git branches for branch preparation."
            },
            "outFiles": {
              "type": "array",
              "items": { "type": "string" },
              "markdownDescription": "Replaces the default `outFiles` glob list in generated attach configurations. Use this when the standard CAP folder layout (`srv`, `gen/srv`, `app`, `lib`, `dist`, `build`) does not match your project."
            },
            "outFilesExtra": {
              "type": "array",
              "items": { "type": "string" },
              "markdownDescription": "Appended to the default `outFiles` glob list. Ignored when `outFiles` is set. Useful for adding extra build folders without redefining the entire list."
            },
            "resolveSourceMapLocations": {
              "type": ["array", "null"],
              "items": { "type": "string" },
              "markdownDescription": "Overrides VS Code's `resolveSourceMapLocations`. Defaults to `null` so source maps with embedded remote paths (e.g. `/home/vcap/app/...`) are not silently dropped during attach. Set an explicit list of globs to re-enable filtering."
            },
            "sourceMapPathOverrides": {
              "type": "object",
              "additionalProperties": { "type": "string" },
              "markdownDescription": "Path-mapping overrides merged on top of the extension defaults. Keys are the source-map embedded paths, values are the local replacements (e.g. `\"/home/vcap/app/*\": \"${workspaceFolder}/*\"`)."
            }
          }
        }
      }
    },
    "commands": [
      {
        "command": "cdsDebug.resetConfig",
        "title": "CDS Debug: Reset Configuration"
      },
      {
        "command": "cdsDebug.stopAllSessions",
        "title": "CDS Debug: Stop All Debug Sessions"
      },
      {
        "command": "cdsDebug.clearCredentials",
        "title": "CDS Debug: Clear Saved Credentials"
      },
      {
        "command": "cdsDebug.showWhatsNew",
        "title": "CDS Debug: What's New"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "node esbuild.mjs",
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "package": "vsce package --no-dependencies",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test --max-warnings=0",
    "cspell": "cspell '**/*.{ts,json,md}' --no-progress",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.5.2",
    "@types/vscode": "1.110.0",
    "@vitest/coverage-v8": "^4.1.2",
    "@vscode/vsce": "^3.9.1",
    "cspell": "^10.0.0",
    "esbuild": "^0.28.0",
    "eslint": "^10.3.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.0.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1",
    "vite": "7.3.2",
    "vitest": "^4.1.2"
  },
  "packageManager": "pnpm@9.15.0",
  "pnpm": {
    "overrides": {
      "vite": "7.3.2"
    }
  },
  "dependencies": {
    "@saptools/cf-sync": "^0.4.6"
  }
}
